# @MalenkyiVsesvitBot

Два незалежні модулі в одному боті (один токен - один процес polling):

1. **Прикорм** - щоденник харчування дитини, бібліотека продуктів, тарілки, рецепти,
   реакції, зростання за ВООЗ, нагадування. Модуль `feed/`, контент у `data/`.
2. **YouTube downloader** - кинь посилання, обери якість, отримай файл. `/yt`.


## Прикорм

`/start` - головне меню. Керування кнопками.

| Екран | Що робить |
|---|---|
| 🍽 Записати прийом їжі | Конструктор тарілки: «Часте» або групи продуктів → до 6 позицій → «Подали». Підказує, чого не вистачає до балансу (рослинне / вуглевод / білок / жир) |
| 🧊 Холодильник | Що є вдома. Складає тарілки тільки з наявного і рахує **закупівлю на тиждень уперед**. Продукти додаються тапом або одним рядком «морква, гречка, індичка» |
| 🥕 Продукти | 202 картки: з якого віку, алерген, ризик подавитися, **як подавати саме зараз** за віком дитини, користь, застереження. Позначки ✅ 🆕 👀 ⛔ ⏳, алерген позначено ❗ |
| 🍲 Тарілки | 30 готових збалансованих комбінацій за віком (6-8 / 9-11 / 12+). Кнопка «Подали цю тарілку» кидає весь склад у щоденник. «🎲 Ще варіанти» збирає нові без обмеження, «✍️ У мене вже є…» добирає те, чого бракує |
| 👨‍🍳 Рецепти | 41 рецепт: інгредієнти з вагою, кроки, порада, як заморожувати. Екран пояснює, скільки підходить зараз і коли відкриється решта. «🔍 Знайти за продуктом» шукає рецепти з конкретним інгредієнтом, «🧊 Що зварити з наявного» показує те, на що вже все є вдома |
| 📔 Щоденник | День за днем, нотатки, видалення. «Зведення» - прогрес по групах і скільки топ-алергенів уже введено |
| 📈 Зростання | Вага і зріст → перцентиль і z-score за офіційними таблицями ВООЗ, з коридором норми |
| ℹ️ Інструкція | `/help` - сім екранів довідки: з чого почати, записи, холодильник, продукти, алергени, зростання, родина і дані. Доступна ще до створення профілю |
| ⚙️ Налаштування | Код запрошення для другого дорослого, вимикач нагадувань, зміна дати народження, дані і приватність, видалення всіх даних |

### Полиці білка

Мʼясо, риба, яйця і бобові лежать на окремих полицях (`sub:` у `products.yaml`),
бо шукати індичку серед сорока шести позицій незручно. Для балансу тарілки це
один слот: `group` лишається `protein`, і генератор рахує їх разом.

### Генератор тарілок

`feed/suggest.py`. Тарілка вважається зібраною, коли закриті чотири слоти:
рослинне, вуглевод, білок, жир.

- **🎲 Ще варіанти** - нові комбінації з продуктів, дозволених за віком і не
  позначених «уникаємо». Знайоме важить утричі більше за нове, і в одній тарілці
  максимум один ще не введений продукт.
- **✍️ У мене вже є…** - пишеш «морква і курка», бот розпізнає продукти в тексті
  і добирає решту. Працює і без кнопки: будь-який текст із назвами продуктів
  веде в добір.

Розпізнавання - основа слова плюс словник синонімів (`ALIASES`): «картошка»,
«омлет», «сьомга», «творог», «паста» знаходяться. Спершу перевіряється пара
сусідніх слів («зелена сочевиця», «твердий сир»), потім точний збіг одного слова
по всьому каталогу, і лише потім збіг за початком. Стиль страви (`style:` у
`products.yaml`: main / breakfast / both) не дає зліпити лосося з грушею.
Картопля і батат закривають слот вуглеводу замість рослинного, інакше генератор
видає картоплю з макаронами. Цибуля і часник з генерації виключені - це приправа.
Продукти з `plate: false` (ковбаса, солодощі, снеки, чіпси) не потрапляють ні в
згенеровану тарілку, ні в список покупок: записати з'їдене вручну можна, радити - ні.

### Холодильник

Запас родини (`pantry` у базі, спільний для обох дорослих):

- **🍲 Скласти тарілку з цього** - той самий генератор, обмежений наявним удома.
  Слот, під який нічого немає, лишається порожнім, і бот каже, чого бракує.
- **🛒 Список покупок на тиждень** - `suggest.shopping()`. Тижнева норма:
  6 різних рослинних, 3 вуглеводи, 4 білки, 2 жири, а всередині білка ще й
  різноманіття (2 мʼясо, 1 риба, 1 яйця, 1 бобові). Віднімає те, що вже вдома,
  спершу пропонує те, що дитина вже їсть, і додає один новий продукт та один
  ще не введений алерген.
- **✍️ Списком** - до 20 продуктів одним повідомленням через кому.
- Продукт, що лежить понад тиждень, підсвічується кількістю днів.

### Публічний доступ

Бот відкритий: `ALLOWED_USERS` порожній, будь-хто може завести собі профіль дитини.
Дані ізольовані по родинах.

⛔ **Качалка з YouTube відкритою НЕ є.** Вона їсть диск і трафік VPS, тому має
власний список `YT_USERS` у `.env`. Порожній `YT_USERS` означає «діє
`ALLOWED_USERS`», тобто теж усім - для публічного бота так лишати не можна.

Код запрошення в родину живе **добу** і одноразовий: бот публічний, а код це
ключ до щоденника чужої дитини.

Профіль бота (назва, опис, коротке пояснення, меню команд) виставляється скриптом:

```bash
python3 tools/bot_profile.py           # показати, що зараз і що стане
python3 tools/bot_profile.py --apply   # записати в Telegram
```

Назва продукту: **Прикорм · Маленький Всесвіт**. Функція попереду, бо в пошуку
Telegram набирають «прикорм»; бренд позаду, бо він уже в юзернеймі. Telegram
обмежує зміну назви кількома разами на добу, тому скрипт чіпає її лише коли
вона справді інша.

`/yt` навмисно не в публічному меню команд: він видається окремим списком
тільки тим, хто в `YT_USERS`.

### База підписників

Кожен, хто торкнувся бота, потрапляє в таблицю `subscriber`: id, username, імʼя,
мова, джерело (payload deep-link `/start?start=...`), перший і останній візит,
кількість візитів, ознака «заблокував бота». Профіль дитини заводять не всі, а
знати аудиторію треба з першого дня.

```bash
python3 tools/feed_subs.py stats                        # зведення
python3 tools/feed_subs.py export --out subs.csv        # вивантаження
python3 tools/feed_subs.py broadcast --text msg.txt     # СУХА прогонка
python3 tools/feed_subs.py broadcast --text msg.txt --send
```

Розсилка за замовчуванням суха. Хто заблокував бота, помічається `blocked=1` і
в наступну розсилку не потрапляє. Видалення даних родини підписника **не**
стирає: людина просила видалити щоденник дитини, а не піти з бота.

### Пошук рецептів

`catalog.recipes_with(codes)` шукає за кодами продуктів, розібраними з тексту тим
самим `suggest.parse_products`, що й «у мене вже є». Ранжування за кількістю
збігів.

Якщо рецепта саме з цим продуктом немає, `_same_shelf()` пропонує сусідів по
полиці: на «курка» знайдеться індичка. Порожній екран без пояснення це найгірше,
що може віддати пошук.

`catalog.recipe_missing(rid, stock)` рахує, чого бракує з холодильника. Звідси
два блоки на екрані «🧊 Що зварити з наявного»: «усе є» і «бракує однієї позиції».

### Спільний профіль

Один профіль дитини на кількох дорослих. Перший створює профіль, далі
**Налаштування → Додати дорослого** видає одноразовий код. Другий відкриває бота,
тисне «У мене є код запрошення», надсилає код - і пише в той самий щоденник.

Якщо `ALLOWED_USERS` заповнений, **створити нову родину** можуть тільки ці user_id,
а приєднатися за кодом - будь-хто, кого запросили. Свій id: `/id`.

### Нагадування

| Коли | Що |
|---|---|
| 20:30 того ж дня | «Як пройшло знайомство з продуктом?» → ✅ Все добре / ⚠️ Була реакція |
| через 3 дні | Правило трьох днів: продукт без реакцій → пропонує позначити освоєним |
| 21:00 щодня | Якщо за день немає жодного запису |

Скан черги кожні 10 хв, таймзона Europe/Kyiv. Вимикаються в налаштуваннях.

### Дані і контент

| Файл | Що |
|---|---|
| `data/products.yaml` | 202 продукти: група, вік, алерген, ризик задухи, подача 6/9/12 міс, користь, застереження |
| `data/plates.yaml` | 30 ідей тарілок за віком |
| `data/recipes.yaml` | 41 рецепт |
| `data/who.json` | LMS-таблиці ВООЗ (вага і зріст, хлопчики і дівчатка, 0-5 років, подобово) |
| `data/who_src/*.xlsx` | оригінали ВООЗ, з яких зібрано who.json |
| `var/feed.sqlite3` | база родин і підписників. **Персональні дані дитини, у git не йде** |

Перезібрати таблиці ВООЗ: `python3 tools/build_who.py`.

Контент - довідка на основі рекомендацій ВООЗ, AAP і EFSA. Не медичне призначення.

### Перевірка після правок

```bash
python3 tools/feed_smoke.py     # прогін усіх 474 екранів на тимчасовій базі
systemctl --user restart ytbot
```

Смоук перевіряє: інструкцію, онбординг, конструктор, усі 202 картки, усі 30 тарілок
і 18 рецептів, реакції, холодильник зі списком покупок, щоденник, зростання,
спільний профіль, нагадування, базу підписників і видалення даних, а також ліміти
Telegram (4096 символів на повідомлення, 64 байти на callback_data, розмір
клавіатури) і валідність HTML.


## YouTube

## Керування

```bash
systemctl --user status ytbot          # стан
systemctl --user restart ytbot         # рестарт після правок bot.py
journalctl --user -u ytbot -f          # логи наживо
```

Автостарт увімкнено (`enable`), `Restart=always`, linger=yes → переживає і краш, і ребут.

## Налаштування — `~/ytbot/.env`

| Змінна | Що робить |
|---|---|
| `BOT_TOKEN` | токен бота |
| `LOCAL_API_URL` | порожньо = хмарний API (50 МБ); `http://127.0.0.1:8081` = локальний (2000 МБ) |
| `MAX_UPLOAD_MB` | ліміт віддачі: 50 для хмарного, 2000 для локального |
| `MAX_DURATION_MIN` | відсікає задовгі відео (типово 180) |
| `MAX_PARALLEL` | скільки завантажень одночасно (2 ядра → 2) |
| `ALLOWED_USERS` | порожньо = бот відкритий усім; список user_id = whitelist |
| `COOKIES_FILE` | шлях до cookies.txt, якщо YouTube почне вимагати логін |

## Ліміт 50 МБ → 2 ГБ

Хмарний Bot API не віддає файли більші за 50 МБ. Знімається локальним
`telegram-bot-api`. Що треба:

1. `sudo apt install -y cmake libssl-dev zlib1g-dev gperf`
2. `api_id` + `api_hash` з https://my.telegram.org → вписати в `~/ytbot/local-api.env`
3. `nohup ~/ytbot/setup-local-api.sh > ~/ytbot/build.log 2>&1 &` (збірка ~1.5-3 год на 2 ядрах)
4. `systemctl --user enable --now telegram-bot-api`
5. У `.env`: `LOCAL_API_URL=http://127.0.0.1:8081` і `MAX_UPLOAD_MB=2000`,
   далі `systemctl --user restart ytbot`

Увага: при переході на локальний сервер Telegram вимагає `logOut` бота з хмарного
сервера один раз:
`curl "https://api.telegram.org/bot<TOKEN>/logOut"` — і тільки після цього
локальний сервер прийме токен.

Кеш локального сервера лежить у `~/ytbot/tgapi-data` і росте — час від часу чистити.

## Диск

Тимчасові файли — `/tmp/ytbot`, чистяться після кожної віддачі. Вільно зараз ~13 ГБ.
